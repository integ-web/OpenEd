
  # OpenEd(v1)

  This is a code bundle for OpenEd(v1). The original project is available at https://www.figma.com/design/sHMH5uvqdYtQYV3qB8CVkp/OpenEd-v1-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  